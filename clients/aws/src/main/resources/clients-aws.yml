cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    region:
      static: ap-northeast-2
    s3:
      bucket-name: namu-bucket
      localstack:
        endpoint: http://localhost:4566
        bucket-name: localstack-bucket
